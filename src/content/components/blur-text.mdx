---
order: 0
title: "Blur Text"
description: "Linear-style text blurring animation."
lastModifiedAt: 2024-06-18
publishedAt: 2024-06-18
---

import ReactBlurText from "../../components/blur-text/blur-text.tsx";
import SvelteBlurText from "../../components/blur-text/blur-text.svelte";
import VueBlurText from "../../components/blur-text/blur-text.vue";

import reactCode from "../../components/blur-text/blur-text.tsx?raw";
// import svelteCode from "../../components/blur-text/blur-text.svelte?raw";
// import vueCode from "../../components/blur-text/blur-text.vue?raw";

# Blur Text

Text entry animation inspired by the new [linear](https://linear.app/) landing page.

## Preview

<div className="">
  {props.framework === "react" && (
    <ReactBlurText
      text="This is a React component. Linear is a purpose-built tool for planning and building products"
      className="text-4xl font-semibold"
    />
  )}
  {props.framework === "vue" && (
    <VueBlurText
      text="This is a Vue component. Linear is a purpose-built tool for planning and building products"
      className="text-4xl font-semibold"
    />
  )}
  {props.framework === "svelte" && (
    <SvelteBlurText
      text="This is a svelte component. Linear is a purpose-built tool for planning and building products"
      className="text-4xl font-semibold"
    />
  )}
</div>

## Install

Copy the following code to your project:

```tsx
import { cn } from "@utils/cn";

type BlurTextProps = {
  text: string;
  startDelay?: number;
  className?: string;
};

export default function BlurText({
  text,
  startDelay = 0,
  className,
  ...props
}: BlurTextProps) {
  const words = text.split(" ");

  return (
    <span className="text-balance text-center">
      {words.map((word, index) => (
        <span
          key={word}
          className={cn("inline-block animate-blur-text opacity-0", className)}
          style={{ animationDelay: `${index * 60 + startDelay}ms` }}
          {...props}
        >
          {word}
          {index < words.length - 1 && <span>&nbsp;</span>}
        </span>
      ))}
    </span>
  );
}
```

<figure>

</figure>

{/* {props.framework === "react" && <Code code={reactCode} lang="tsx" title="src/components/blur-text.tsx" />}
{props.framework === "svelte" && <Code code={svelteCode} lang="svelte" title="src/components/blur-text.svelte" />}
{props.framework === "vue" && <Code code={vueCode} lang="vue" title="src/components/blur-text.vue" />} */}
